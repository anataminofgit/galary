
const initialmgs = {imagesList: []};

const imageReducer = (state = initialmgs, action) => {

    switch (action.type) {
        // case XXX:
        //     {
        //         return {
        //             ...state,
        //             imagesList: [...action.imagesList]
        //         };
        //     }
        default:
            return state;
    }
};
export default imageReducer;